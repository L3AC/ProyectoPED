<UserControl x:Class="ProyectoPED.Views.AVLTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="400">

    <Border Background="#F8F9FF">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#4D4D8B">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Tree" Width="22" Height="22" Foreground="White" Margin="0,0,10,0"/>
                        <TextBlock Text="Árbol AVL - Tareas" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                    <Button Content="✕" HorizontalAlignment="Right" Margin="0,0,15,0"
                            Background="Transparent" Foreground="White" BorderThickness="0"
                            FontSize="18" Cursor="Hand" Click="BtnCerrar_Click" Padding="10,5"/>
                </Grid>
            </Border>

            <!-- Área del árbol con scroll -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                          Background="#F8F9FF">
                <Canvas x:Name="AVLCanvas" Background="#F8F9FF" Width="800" Height="600">
                    <Canvas.Resources>
                        <Style TargetType="Border" x:Key="NodeBorderStyle">
                            <Setter Property="BorderBrush" Value="#4B5563"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="CornerRadius" Value="12"/>
                            <Setter Property="Width" Value="110"/>
                            <Setter Property="MinHeight" Value="55"/>
                        </Style>
                    </Canvas.Resources>
                </Canvas>
            </ScrollViewer>

            <!-- Botones inferiores -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20,15">
                <Button Content="🔄 Actualizar árbol" Click="BtnActualizar_Click" 
                        Background="#4D4D8B" Foreground="White" BorderThickness="0"
                        FontSize="14" FontWeight="SemiBold" Cursor="Hand"
                        Padding="30,8" Margin="0,0,20,0" Height="38">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#3D3D6B"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="✕ Cerrar panel" Click="BtnCerrar_Click" 
                        Background="Transparent" Foreground="#4B5563" BorderThickness="2" BorderBrush="#D1D5DB"
                        FontSize="14" FontWeight="SemiBold" Cursor="Hand"
                        Padding="30,8" Height="38">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#F3F4F6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
            
            <!-- Info del árbol -->
            <Border Grid.Row="2" Background="#EEF2FF" Padding="15,10" Margin="20,0,20,70" CornerRadius="8">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock x:Name="TxtInfoNodos" Text="Nodos: 0" FontSize="12" Foreground="#4B5563" Margin="0,0,20,0"/>
                    <TextBlock x:Name="TxtInfoAltura" Text="Altura: 0" FontSize="12" Foreground="#4B5563" Margin="0,0,20,0"/>
                    <TextBlock x:Name="TxtInfoBalance" Text="Balance: OK" FontSize="12" Foreground="#4B5563"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
