<Window x:Class="ProyectoPED.Views.SimulationResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Resultado de la Simulación" Height="560" Width="520"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="#F3F4F6">

    <Grid>
        <Border Background="White" CornerRadius="12" Margin="20">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="3" BlurRadius="15" Opacity="0.15" Color="#000000"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#8B5CF6" CornerRadius="12,12,0,0" Padding="25,20">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ChartTimelineVariant" Width="28" Height="28" Foreground="White" Margin="0,0,12,0"/>
                        <StackPanel>
                            <TextBlock x:Name="TxtTitulo" Text="Resultado de la Simulación" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,20">
                    <StackPanel>
                        <!-- Resumen -->
                        <Border Background="#EEF2FF" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <materialDesign:PackIcon Kind="CalendarToday" Foreground="#6366F1" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="TxtFechaSimulada" FontSize="14" FontWeight="SemiBold" Foreground="#4338CA"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ArrowRightBold" Foreground="#6366F1" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="TxtDiasAvanzados" FontSize="13" Foreground="#6366F1"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Estado de tareas -->
                        <Border Background="#FAFAFA" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="Estado de Tareas" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,12"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#D1FAE5" CornerRadius="8" Padding="12" Margin="0,0,6,0">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock x:Name="TxtCompletadas" TextAlignment="Center" FontSize="24" FontWeight="Bold" Foreground="#059669"/>
                                            <TextBlock Text="Completadas" TextAlignment="Center" FontSize="12" Foreground="#047857" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Grid.Column="1" Background="#FEF3C7" CornerRadius="8" Padding="12" Margin="6,0,0,0">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock x:Name="TxtPendientes" TextAlignment="Center" FontSize="24" FontWeight="Bold" Foreground="#D97706"/>
                                            <TextBlock Text="Pendientes" TextAlignment="Center" FontSize="12" Foreground="#B45309" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Tareas Vencidas -->
                        <Border x:Name="CardVencidas" Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <materialDesign:PackIcon Kind="AlertCircle" Foreground="#DC2626" Width="20" Height="20" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtVencidasTitulo" FontSize="15" FontWeight="SemiBold" Foreground="#991B1B"/>
                                </StackPanel>
                                <ItemsControl x:Name="ListaVencidas">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#FEE2E2" CornerRadius="6" Padding="12,8" Margin="0,0,0,6">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding Titulo}" FontSize="13" FontWeight="Medium" Foreground="#991B1B"/>
                                                    <Border Grid.Column="1" Background="#DC2626" CornerRadius="4" Padding="8,3">
                                                        <TextBlock Text="{Binding DiasRestantes, StringFormat={}{0} días}" FontSize="11" FontWeight="Bold" Foreground="White"/>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock x:Name="TxtSinVencidas" Text="No hay tareas vencidas" FontSize="13" Foreground="#6B7280" FontStyle="Italic" Visibility="Collapsed"/>
                            </StackPanel>
                        </Border>

                        <!-- Próximas a vencer -->
                        <Border x:Name="CardProximas" Background="#FFF7ED" BorderBrush="#FED7AA" BorderThickness="1" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <materialDesign:PackIcon Kind="ClockAlert" Foreground="#EA580C" Width="20" Height="20" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtProximasTitulo" FontSize="15" FontWeight="SemiBold" Foreground="#9A3412"/>
                                </StackPanel>
                                <ItemsControl x:Name="ListaProximas">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#FFEDD5" CornerRadius="6" Padding="12,8" Margin="0,0,0,6">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding Titulo}" FontSize="13" FontWeight="Medium" Foreground="#9A3412"/>
                                                    <Border Grid.Column="1" Background="#EA580C" CornerRadius="4" Padding="8,3">
                                                        <TextBlock Text="{Binding Severidad}" FontSize="11" FontWeight="Bold" Foreground="White"/>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock x:Name="TxtSinProximas" Text="No hay tareas próximas a vencer" FontSize="13" Foreground="#6B7280" FontStyle="Italic" Visibility="Collapsed"/>
                            </StackPanel>
                        </Border>

                        <!-- Recomendaciones -->
                        <Border x:Name="CardRecomendaciones" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="10" Padding="20" Margin="0,0,0,15"
                                Visibility="Collapsed">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <materialDesign:PackIcon Kind="LightbulbOn" Foreground="#2563EB" Width="20" Height="20" Margin="0,0,8,0"/>
                                    <TextBlock Text="Recomendaciones" FontSize="15" FontWeight="SemiBold" Foreground="#1E40AF"/>
                                </StackPanel>
                                <ItemsControl x:Name="ListaRecomendaciones">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#DBEAFE" CornerRadius="6" Padding="12,10" Margin="0,0,0,6">
                                                <TextBlock Text="{Binding}" FontSize="13" Foreground="#1E3A5F" TextWrapping="Wrap"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="2" Background="#F9FAFB" CornerRadius="0,0,12,12" Padding="25,15">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Aceptar" Padding="30,8"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Background="#8B5CF6"
                                Click="BtnAceptar_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
